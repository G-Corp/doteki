{application, doteki, [
	  {description, "Dynamic configuration in your erlang application"}
	, {vsn, "1.1.0"}
	, {modules, []}
	, {registered, []}
	, {applications, [kernel,stdlib]}

  , {licenses, ["BSD-3"]}
  , {links, [{"Github", "https://github.com/G-Corp/doteki"}]}
  , {files, ["src/*"
             , "lib/*"
             , "Makefile"
             , "bu.mk"
             , "README.md"
             , "rebar.config"
             , "rebar.lock"
             , "mix.exs"
             , "mix.lock"]}
  , {build_tools, ["rebar3", "mix", "make"]}
]}.
